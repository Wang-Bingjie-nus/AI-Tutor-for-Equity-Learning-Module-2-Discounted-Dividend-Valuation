services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-equity-tutor
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
    restart: unless-stopped